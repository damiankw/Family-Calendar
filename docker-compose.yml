version: "3.8"

services:
  family-calendar:
    build: .
    ports:
      - "3000:3000"
    environment:
      DB_PATH: /data/family-calendar.db
    volumes:
      - ./data:/data
    restart: unless-stopped
